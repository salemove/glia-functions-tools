---
list_response:
  type: object
  properties:
    scheduling_policies:
      type: array
      description: List of queue scheduling policies.
      items:
        type: object
        properties:
          $ref: './scheduling_policy.yaml#/properties'

limited_list_response:
  properties:
    scheduling_exception_policies:
      type: array
      description: List of scheduling policies with limited information.
      items:
        $ref: '../../../common_components/properties.yaml#/list_limited_response'
